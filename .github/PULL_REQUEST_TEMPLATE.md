## 这个改动解决什么

<!-- 一句话说明动机。纯重构也请写清「为什么现在要做」 -->

## 怎么改的

<!-- 关键取舍写在这里；不解释逐行代码 -->

## 验证

- [ ] `php tests/unit.php` 全绿：结果 `____ 通过, 0 失败`
- [ ] `php tests/http.php` 全绿：结果 `____ 通过, 0 失败`
- [ ] 新增/修改的行为有对应断言
- [ ] 所有 PHP 文件 `php -l` 通过（unit.php 的 D1/D2 会覆盖）
- [ ] 涉及模板或交互行为时，已在浏览器里跑过 `tests/app` 夹具

<!-- 断言数字请如实填写，跑不过就别提 -->

## 兼容性与文档

- [ ] 兼容性改动：小版本号 + `klockframe.php` 头部条目 + `CHANGELOG.md`
- [ ] 破坏性改动：主版本号 + `docs/migration-<major>.md`（改名对照 + 行为差异 + 自查 grep）
- [ ] `docs/api-reference.md` 与相关文档已同步；被删的函数在文档/示例/测试里已清干净
- [ ] 应用侧新接口只出现 `kf_` 前缀（不引导业务代码直呼 `xn_*` / `param()`）
