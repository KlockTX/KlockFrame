# 安全策略

## 报告漏洞

请**不要**开公开 issue。用仓库的 GitHub 私有漏洞报告（Security → Reports）提交，
或在无法使用时发邮件给维护者（GitHub 个人页公开的邮箱均可）。

报告里请尽量给出：受影响版本、复现步骤、是否已被利用的迹象、建议的修复方向。
我们会在 7 天内回复确认与预估修复时间。

## 受支持版本

| 版本 | 支持状态 |
|------|----------|
| 2.0.x | ✅ 支持 |
| 1.x | ❌ 停止维护（未纳入 git，修复只进 2.0） |

## 框架已经替你挡住的

| 风险 | 框架内的处置 |
|------|--------------|
| SQL 注入 | `db_*` 全链路 PDO 预处理，条件与排序字段不接受拼接 |
| XSS（输出侧） | `KF_Element` 对文本与属性值一律 `htmlspecialchars`；原样输出必须显式 `kf_raw()` |
| 属性注入 | 属性名需匹配 HTML 命名规则才渲染，值中的引号被实体化，无法逃出属性上下文 |
| 响应拆分注入 | `kf_response_header()` 拒绝非法头名，并**整条拒绝含 CR/LF 的头值**（不抹掉换行后照发） |
| CSRF | `kf_csrf_field()` / `kf_csrf_check()`；局部刷新请求由 `kf_runtime()` 自动附带 CSRF 头，服务端用 `kf_csrf_ok()` 校验 |
| 路径穿越（zip） | `kf_unzip()` 走 `xn_zip_entry_is_safe()`，含越界条目的包整体拒绝且不部分落盘 |
| 运行时被替换 | 内置路由直出前校验 `js/htmx.min.js` 的 sha256，不匹配则 500 且不吐任何字节 |
| 控制器名注入 | 路由 handler `控制器@方法` 两侧都做白名单校验后才参与文件路径与函数名构造 |
| 日志被直接下载 | 日志落为 `.php` 且首行写 `<?php exit;?>` 守卫；缺守卫的历史文件会在写入时原子补上 |
| IP 伪造 | `kf_ip()` 严格可信代理模式：直连地址不在可信代理表时，不采信 `X-Forwarded-For` |

## 需要你负责的

这些框架管不了，部署时请自查：

1. **`auth_key` / 数据库口令不得入库**，`.gitignore` 已排除 `config.php`，请不要关掉这条规则
2. **`allowEval`**：交互运行时默认允许 `hx-vals="js:…"`、`hx-on` 执行 JS。
   若页面上会把用户输入写进这些属性，请配置 `'app' => ['htmx' => ['config' => ['allowEval' => false]]]`
3. **会话与传输**：生产环境启用 HTTPS 与安全的 session 存储（框架不管理 session 生命周期）
4. **Web 服务器规则**：站点目录需禁止直接访问 `.php` 结尾的数据/日志文件之外的敏感路径；
   `data/` 一类目录请配合 `.htaccess` 或等价 Nginx 规则
5. **最小权限**：数据库账号不要给 `DROP`；上传目录不要给执行权限
6. **依赖版本**：内置运行时版本由 `KF_RUNTIME_VERSION` 标注，跟进上游安全公告时以此为准

## 已知的非漏洞

- 局部刷新的片段响应默认可被缓存策略影响，需要禁缓存时显式调用 `kf_no_cache()`
- `kf_dump()` 只在 `DEBUG` 为真时输出；生产环境请确保 `DEBUG` 关闭
